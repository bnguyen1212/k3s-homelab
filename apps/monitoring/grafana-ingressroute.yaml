apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`grafana.bnguyen.dev`)
      kind: Rule
      services:
        - name: kube-prometheus-grafana
          port: 80
